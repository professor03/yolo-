# 貢獻指南

先閱讀 README 與 docs/architecture.md，再使用 Python 3.12 的獨立虛擬環境。
正式入口為 scripts/serve_demo.py 與 scripts/detect_demo.py。

修改後執行 README 所列公開測試。涉及推論時再用有權使用的本機影片驗證，
記錄模型、設定、硬體與處理影格數。測試不可連到既有攝影機。

請在 PR 描述問題、行為變更、驗證結果及限制；避免順手重寫無關模組。
不要提交 .env、真實帳密、攝影機網址、資料庫、影像、權重、日誌或備份。
模型準確率與個人貢獻需有實際證據，勿用示範訊號代替真實測量。

回報一般問題可使用 GitHub Issues，請提供重現步驟及已去識別化的錯誤訊息。
